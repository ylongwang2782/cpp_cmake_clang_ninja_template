{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/CppCmakeClangNinjaTemplate.exe",
            "cwd": "${workspaceFolder}",
            "args": [],
            "preLaunchTask": "build"
        }
    ]
}