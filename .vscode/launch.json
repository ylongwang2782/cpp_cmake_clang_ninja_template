{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/debug/CppCmakeClangNinjaTemplate.exe",
            "cwd": "${workspaceFolder}",
            "args": [],
            "preLaunchTask": "build"
        }
    ]
}